# Ultimate Python Collections Practice Suite

This practice suite contains targeted questions designed to challenge your understanding of Python strings, sequences, and mappings. Work through these exercises to test your edge-case handling, structural reasoning, and performance awareness.

---

## 1. Strings, Indexing & Slicing

1. **The Step Index Reverse:** Given the string `payload = "STATUS_ERR_2026_CRITICAL"`, write a single slicing expression that extracts the year `"2026"` by counting backward using negative step bounds.
2. **Out of Bounds Safety:** What happens when you attempt to access `text[50]` on a string of length 10? How does this behavior change if you use a slicing expression like `text[5:50]` instead?
3. **Stride Isolation:** Given a string `data = "A1B2C3D4E5"`, construct a slice that isolates only the hidden alphabetical characters (`"ABCDE"`) and another slice that isolates only the numeric digits (`"12345"`).
4. **Immutable Reassignment Attempt:** Explain what happens under the hood if you execute the following block:
```python
router_id = "rt-01"
router_id[3] = "2"

```


5. **The Empty Slice:** Given `word = "Python"`, what is the exact return value of `word[4:2]` and `word[4:2:-1]`? Explain the structural logic behind both evaluations.

---

## 2. String Methods

6. **Whitespace Normalization:** Write an expression to clean an unformatted log entry string `raw_log = " \n\t [SYSTEM ALERT]: Connection Timeout  \r "` so that it outputs exactly `"[SYSTEM ALERT]: Connection Timeout"`.
7. **Safe Substring Location:** Explain the precise functional difference between using `text.find("target")` and `text.index("target")` when the substring `"target"` is completely missing from the host string.
8. **Delimiter Splitting Boundaries:** Given the string `csv_row = "alpha,,beta,gamma,"`, what is the exact length and composition of the list returned by running `csv_row.split(",")`?
9. **Dynamic Padding Validation:** You need to format numerical transaction counter numbers to always display as 8-character strings padded with leading zeros (e.g., `45` becomes `"00000045"`). Which native string method provides this behavior out of the box?
10. **Prefix/Suffix Sanity:** Given an asset handle string `filename = "backup_2026_07.tar.gz"`, outline how to cleanly verify that the file starts with `"backup_"` and ends with `".gz"` in a single conditional statement without using regular expressions.

---

## 3. Lists

11. **Dynamic Resizing Overhead:** When you continuously use `.append()` to add items to a growing Python list, does the underlying C engine allocate new memory space for every single element? Explain the performance implications.
12. **In-Place Sorting vs. Copy Mutation:** Contrast the behavioral and structural differences between calling `my_list.sort()` and calling the built-in function `sorted(my_list)`. What do they return, and how is the original memory address affected?
13. **Shallow Pointer Hazards:** Examine the following execution path:
```python
matrix_a = [[1, 2], [3, 4]]
matrix_b = list(matrix_a)
matrix_a[0][0] = 99

```


Predict the exact contents of `matrix_b` and explain why the value modification leaked across the two variables.
14. **Element Shift Cost:** Why is using `my_list.insert(0, item)` considered a linear time operation ($O(n)$), while using `my_list.append(item)` is an amortized constant time operation ($O(1)$)?
15. **Slice Modification:** Given `numbers = [10, 20, 30, 40, 50]`, evaluate what the list contains after running the assignment: `numbers[1:4] = [99]`.

---

## 4. Tuples

16. **Deep Immutability Evaluation:** Consider a tuple defined as `record = ("admin", [10, 20], True)`. Why is this tuple considered physically immutable but structurally volatile? What happens if you run `record[1].append(30)`?
17. **The Single-Element Trap:** Why does `packet = ("data")` evaluate to a string type rather than a tuple? Write the exact syntax required to declare a single-element tuple safely.
18. **Unpacking with Star Wildcards:** Given the record structure `telemetry = ("node_alpha", 0.82, 94.1, 0.0, "ACTIVE")`, write a single unpacking statement that assigns `"node_alpha"` to a variable named `node`, `"ACTIVE"` to a variable named `status`, and aggregates all intermediate float data into a list variable named `metrics`.
19. **Tuple Memory Optimization:** Why do tuples require less memory overhead and execute slightly faster to construct than lists containing the exact same element values?
20. **Tuple Concatenation Mechanics:** Since tuples are immutable, what happens behind the scenes in system memory when you execute `tuple_a += tuple_b`?

---

## 5. Sets

21. **Hashability Constraints:** Why can you add a tuple containing integers to a set, but you cannot add a tuple containing a list? Explain the underlying technical rules of set elements.
22. **Membership Testing Performance:** Why does checking `if item in collection:` execute in constant time ($O(1)$) if the collection is a set, but linear time ($O(n)$) if it is a list?
23. **Mathematical Unions and Intersections:** Given `set_a = {1, 2, 3, 4}` and `set_b = {3, 4, 5, 6}`, deduce the exact outcomes of the following expressions:
* `set_a & set_b`
* `set_a | set_b`
* `set_a - set_b`
* `set_a ^ set_b`


24. **In-Place Set Mutations:** How does the method `set_a.intersection(set_b)` differ functionally from calling `set_a.intersection_update(set_b)`?
25. **Frozen Set Limitations:** What structural advantage does a `frozenset` offer over a standard `set`, and what core capability does it lose?

---

## 6. Dictionaries & CRUD Operations

26. **Key Overwriting Behavior:** What is the outcome of the following variable definition: `mapping = {"id": 101, "status": "pending", "id": 999}`? How does Python resolve the duplicate key declaration?
27. **Safe Deletion Patterns:** Given a dictionary `inventory = {"apples": 50, "bananas": 12}`, write out the keyword syntax to remove `"apples"` without returning its value. Next, outline how to delete `"grapes"` safely without triggering a `KeyError` if it is missing.
28. **Hash Value Lookup Mapping:** Trace the micro-steps Python performs when you query `user_profiles["user_45"]`, focusing on the role of the hash function and indices table.
29. **CRUD Update Operations:** Given an empty configuration pool `config = {}`, write the sequential CRUD code statements to:
* Create a key `"timeout"` matching `30`.
* Read the value of `"timeout"`.
* Update `"timeout"` to `60`.
* Delete `"timeout"` entirely.


30. **Allowed Key Data Types:** Classify which of the following objects are valid dictionary keys: `42`, `"session_id"`, `[1, 2]`, `{"a": 1}`, `(1, 2)`.

---

## 7. Dictionary Methods

31. **Safe Retrieval with Defaults:** If `settings = {"theme": "dark"}` does not contain a key named `"font_size"`, what does `settings.get("font_size")` return? How do you instruct it to return `14` as a fallback instead?
32. **Conditional Initialization Guard:** Explain how `.setdefault()` differs from `.get()`. Write an example snippet demonstrating how `.setdefault()` can initialize an empty list for a missing key and append a value to it in a single clean layout.
33. **Batch Merging Mechanics:** Given `base_config = {"host": "localhost", "port": 80}` and `patch_config = {"port": 8080, "debug": True}`, what is the exact layout of `base_config` after running `base_config.update(patch_config)`?
34. **Pop Mechanics vs. PopItem:** Compare `d.pop("key")` against `d.popitem()`. What parameters do they accept, what exactly do they return, and how do they manage the underlying dictionary state?
35. **The Memory View Contract:** Why is iterating over `d.items()` highly efficient for large datasets? What limitations exist if you try to modify the dictionary's size inside that loop?

---

## 8. Nested Dictionaries

36. **Chained Bracket Lookup Failures:** Given `payload = {"server": {"status": "healthy"}}`, write an expression to access the `status` value. What happens if the intermediate key `"server"` is completely missing from the dictionary?
37. **Defensive Nested Navigations:** Using the chained `.get()` default fallback pattern, write a single line of defensive code to extract the `"zip_code"` out of `user = {"profile": {"location": {"city": "Austin"}}}` that completely avoids a `KeyError` crash even if `profile` or `location` are absent.
38. **Deep Struct Duplication Errors:** Examine this profile update implementation:
```python
default_settings = {"network": {"dns": "8.8.8.8"}}
session_alpha = default_settings.copy()
session_alpha["network"]["dns"] = "1.1.1.1"

```


Explain why `default_settings["network"]["dns"]` was altered by this operation, and provide the correct architectural fix.
39. **Recursive Tree Construction:** Explain how `collections.defaultdict` can be configured recursively to build an infinitely deep nested dictionary structure on the fly without explicit level-by-level dictionary initializations.
40. **Flattening Mappings Challenge:** Conceptualize an algorithm or approach to flatten a nested dictionary such as `{"a": {"b": {"c": 42}}}` into a single flat key-value map layout: `{"a.b.c": 42}`. Which dictionary view structures are crucial for tracking this data layer?